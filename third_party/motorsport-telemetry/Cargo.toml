[workspace]
resolver = "2"
members = ["bridge", "bridge-header"]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.84"
license = "MIT"
repository = "https://github.com/tobi/omatrack"

[profile.release]
lto = "thin"
codegen-units = 1
overflow-checks = true
